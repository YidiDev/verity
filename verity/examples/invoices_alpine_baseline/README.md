# Manual Alpine Baseline

This directory mirrors the `verity/examples/invoices_alpine` app but swaps the directive-driven data layer for vanilla Alpine stores.

Use the shared instructions in `../invoices_alpine/README.md` for environment setup and run commands.
